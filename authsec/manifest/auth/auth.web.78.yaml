apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: auth.web78
spec:
  selector:
    matchLabels:
      app: web
  action: ALLOW 
  rules:
  - to:
    - operation:
        methods: ["GET"]
        paths: ["/api/cart/add/anonymous-21/Watson/*"]
