apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: auth.1e5fea88-74b3-4a87-acca-902d5cddece7456
spec:
  selector:
    matchLabels:
      app: 1e5fea88-74b3-4a87-acca-902d5cddece7
  action: ALLOW 
  rules:
  - to:
    - operation:
        methods: ["GET"]
        paths: ["/api/fetch/*"]
