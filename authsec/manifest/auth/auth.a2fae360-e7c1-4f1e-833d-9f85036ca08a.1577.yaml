apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: auth.a2fae360-e7c1-4f1e-833d-9f85036ca08a1577
spec:
  selector:
    matchLabels:
      app: a2fae360-e7c1-4f1e-833d-9f85036ca08a
  action: ALLOW 
  rules:
  - to:
    - operation:
        methods: ["GET"]
        paths: ["/api/catalogue/*"]
