apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: auth.cart203
spec:
  selector:
    matchLabels:
      app: cart
  action: ALLOW 
  rules:
  - to:
    - operation:
        methods: ["GET"]
        paths: ["/update/anonymous-39/EMM/*"]
