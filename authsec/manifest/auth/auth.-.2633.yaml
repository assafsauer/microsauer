apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: auth.-2633
spec:
  selector:
    matchLabels:
      app: -
  action: ALLOW 
  rules:
  - to:
    - operation:
        methods: ["GET"]
        paths: ["/nice%20ports%2C/*"]
