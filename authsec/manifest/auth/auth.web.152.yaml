apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: auth.web152
spec:
  selector:
    matchLabels:
      app: web
  action: ALLOW 
  rules:
  - to:
    - operation:
        methods: ["GET"]
        paths: ["/api/cart/update/anonymous-34/HPTD/*"]
