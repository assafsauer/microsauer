apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: auth.3165
spec:
  selector:
    matchLabels:
      app: 
  action: ALLOW 
  rules:
  - to:
    - operation:
        methods: ["GET"]
        paths: ["/shell?cd+/tmp;rm+-rf+*;wget+http*"]
