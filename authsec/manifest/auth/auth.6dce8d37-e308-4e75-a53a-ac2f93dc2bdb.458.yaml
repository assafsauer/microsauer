apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: auth.6dce8d37-e308-4e75-a53a-ac2f93dc2bdb458
spec:
  selector:
    matchLabels:
      app: 6dce8d37-e308-4e75-a53a-ac2f93dc2bdb
  action: ALLOW 
  rules:
  - to:
    - operation:
        methods: ["GET"]
        paths: ["/api/fetch/*"]
