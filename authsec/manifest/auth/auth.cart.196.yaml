apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: auth.cart196
spec:
  selector:
    matchLabels:
      app: cart
  action: ALLOW 
  rules:
  - to:
    - operation:
        methods: ["GET"]
        paths: ["/update/anonymous-32/EMM/*"]
